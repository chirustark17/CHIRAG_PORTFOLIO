import ReelDeck from '../ReelDeck'

// Placeholder: M4 will replace this with vertical-scroll stories.
export default function ReelDeckMobile({ open, onClose }) {
  return <ReelDeck open={open} onClose={onClose} />
}
