import SelectedWork from '../../sections/SelectedWork'

// Placeholder: M3 will replace this with the real mobile SelectedWork.
export default function SelectedWorkMobile() {
  return <SelectedWork />
}
