import Hero from '../../sections/Hero'

// Placeholder: M2 will replace this with the real mobile Hero.
// Until then, mobile users see the desktop Hero component
// (which already has responsive styles).
export default function HeroMobile() {
  return <Hero />
}
